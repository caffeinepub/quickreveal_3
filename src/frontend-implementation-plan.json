{
  "kind": "implementation_plan",
  "version": "1.0",
  "title": "Nexus Singularity AppV1 full overwrite (frontend-only)",
  "requirements": [
    {
      "id": "REQ-298",
      "summary": "Fully overwrite frontend/src/AppV1.jsx with the user-provided Nexus Singularity (Landing + SaaS) code and make only minimal compile/run fixes.",
      "acceptanceCriteria": [
        "`frontend/src/AppV1.jsx` contents are fully replaced by the user-provided code (no partial merges).",
        "The app builds successfully with Vite/TypeScript tooling used by this repo.",
        "The runtime behavior matches the provided code: Landing → Client Explorer and Landing → Pro Onboarding → Pro Cockpit flows render and are navigable as coded.",
        "Persistence uses the same localStorage key as in the provided code: `nexus_v12_clean`."
      ],
      "file_operations": [
        {
          "path": "frontend/src/AppV1.jsx",
          "operation": "modify",
          "description": "Replace the entire file content verbatim with the user-provided “Nexus Singularity (Landing Page + SaaS)” code. Apply only minimal mechanical edits required for this repository to compile/run (e.g., add any missing imports such as framer-motion pieces referenced in the provided code, fix unresolved identifiers/exports/JSX syntax issues without refactoring behavior). Ensure the localStorage key matches `nexus_v12_clean` exactly as in the provided code."
        }
      ]
    },
    {
      "id": "REQ-299",
      "summary": "Preserve existing entry wiring so the app continues to mount via main.tsx and App.tsx stays a thin wrapper rendering AppV1.",
      "acceptanceCriteria": [
        "`frontend/src/App.tsx` remains a thin wrapper that renders `AppV1`.",
        "No changes are made to any frontend immutable paths listed in SYSTEM_CONTEXT.",
        "The application still mounts through `frontend/src/main.tsx` as currently wired."
      ],
      "file_operations": [
        {
          "path": "frontend/src/entry-wiring.md",
          "operation": "create",
          "description": "Add a short developer note documenting the required invariants for this change set: do not modify frontend immutable paths, keep `frontend/src/App.tsx` as a thin wrapper rendering `AppV1`, and keep mounting via `frontend/src/main.tsx` unchanged."
        }
      ]
    },
    {
      "id": "REQ-300",
      "summary": "Keep AppV1 frontend-only by avoiding backend calls and Internet Identity usage within the new AppV1.jsx implementation.",
      "acceptanceCriteria": [
        "No calls to Motoko canister actors are introduced in `frontend/src/AppV1.jsx`.",
        "All state is managed locally using React state and localStorage exactly as in the provided code."
      ],
      "file_operations": [
        {
          "path": "frontend/src/AppV1.jsx",
          "operation": "modify",
          "description": "During the verbatim overwrite, verify the resulting `AppV1.jsx` has no imports/usages of backend actor hooks (e.g., useActor) or Internet Identity hooks/providers, and contains no backend interaction logic; keep all state management purely in-file with React state + localStorage as in the provided code."
        }
      ]
    }
  ]
}